---
title: 发布流程
sort: 6
contributors:
  - d3viant0ne
  - sokra
  - byzyk
  - chenxsan
translators:
  - jsbugwang
  - Yucohny
---

部署 webpack 的发布流程实际上非常简单。请阅读以下步骤，以了解清楚这些是如何完成的。

## 拉取请求（pull request） $#pull-requests$

当向 `main` 分支上提交 pull request 时，选择 **Create Merge Commit** 选项。


## 发布 $#releasing$

```bash
npm version patch && git push --follow-tags && npm publish
npm version minor && git push --follow-tags && npm publish
npm version major && git push --follow-tags && npm publish
```

**这样将递增包版本号，提交变更，然后创建一个本地标签（tag），并推送到 GitHub，然后发布到 npm package**。

之后就可以到 GitHub 的 [发布页面](https://github.com/webpack/webpack/releases) 上为新的标签（tag）编写 Changelog。
